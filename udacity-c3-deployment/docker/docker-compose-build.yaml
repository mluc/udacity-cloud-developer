version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: mluc/reverseproxy
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: mluc/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed  
    image: mluc/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend  
    image: mluc/udacity-frontend:local
